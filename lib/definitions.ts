
export type Stats = {
    value: string;
    label: string;
};
  
export type Product = {
    category: string;
    items: string[];
    description: string;
};

export type Service = {
    title: string;
    items: string[];
    icon: string;
};
  
export type Value = {
    title: string;
    description: string;
    icon: string;
};
  
